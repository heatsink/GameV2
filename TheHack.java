import greenfoot.*;

/**
 * Write a description of class TheHack here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class TheHack extends Actor
{
    
    /**
     * Act - do whatever the TheHack wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        //istrue();
    }  
    /*public boolean istrue(){
        if (Greenfoot.mouseClicked(this) == true || Greenfoot.isKeyDown("p")){
            return true;
        }
        else 
        {
            return false;
        }
    }*/
}
